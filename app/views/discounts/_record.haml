%td= o.name
%td= o.description
%td #{number_with_precision o.amount, precision: 0} %
%td= o.discount_type
%td.actions
  = link_to "Edit", edit_discount_path(o)
  = link_to "Delete", discount_path(o), method: :delete, data: {confirm: 'Are you sure?'}
